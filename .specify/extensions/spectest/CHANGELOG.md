# Changelog

## 1.0.0 (2026-04-10)

- Initial release
- Add `/spectest.generate` command for generating test scaffolds from spec acceptance criteria
- Add `/spectest.coverage` command for mapping spec requirements to test coverage
- Add `/spectest.gaps` command for finding untested spec requirements
- Add `/spectest.plan` command for generating structured test plan documents
- Optional `after_implement` hook for scanning untested requirements
- Supports all major test frameworks and languages
