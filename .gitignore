# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# --- Editors / IDEs ---
.idea/
*.iml
.vscode/
*.swp
*.swo
*~
.\#*
\#*\#

# --- Claude Code / local agent state ---
# Local, machine-specific permission overrides — never share.
.claude/settings.local.json
.claude/*.local.json
CLAUDE.local.md

# --- spec-kit working state ---
# Feature working dirs are generated per-run; commit deliberately, not by default.
specs/*/
!specs/*/spec.md
!specs/*/plan.md
!specs/*/tasks.md

# --- Scala / sbt (future compiled helper-lib, per CLAUDE.md) ---
target/
project/target/
project/project/
.bsp/
.bloop/
.metals/
.scala-build/

# --- JVM / build ---
*.class
*.log
hs_err_pid*

# --- Node (if tooling lands here) ---
node_modules/
npm-debug.log*
yarn-error.log*

# --- Python (if tooling lands here) ---
__pycache__/
*.py[cod]
.venv/
venv/
.env

# --- Secrets / env ---
.env.local
*.pem
*.key
secrets.*
